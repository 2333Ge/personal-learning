- **React 16**：**有优先级、能打断**（render 阶段可暂停），但**优先级不可手动控、中断后要重做、无并发**，属于 “**基础版打断**”。没有完整并发调度和车道模型，渲染一旦开始就必须一次性执行完，**不支持高优先级任务打断低优先级**，只有简单任务排队，容易页面卡顿。
- **React 18**：**全面升级打断能力**，**Lane 细粒度优先级、支持手动标记过渡任务、可并发渲染、可恢复进度**，属于 “**完整版抢占式调度**”。
-